# Titanium を作ってみて

## バックエンドは Firebase を全面的に

Titanium は、バックエンドには全面的に Firebase を導入した。

インフラにお金をかけたくないからである。

<ここに技術的な概要を記載>

### 良いところ

Firestore だけでベクトル類似度検索ができる

### いまいちだったところ

Firestore のルールは正直全くわからない。javascript や Python サブセットで実現できたりしなかったのだろうか。

また Firestore のベクトルストアは 2048 次元が上限なのだが、Firebase AI Logic の API では`gemini-embedding-001`の結果の次元数を指定できず、3072 次元しか得られない。なので適当に間引いて 2048 次元になっている。ここはなぜアウトプットの次元数を指定できないのか。元の Vertex AI の API では

## 生成 AI と Web/アプリ開発の未来予測

SaaS is dead という言葉が言われたり、バイブコーディングで素人でもそれっぽいアプリ画面が作れたりという時代。

これまでも、既存のプログラムの機能追加やリファクタリングにコーディングエージェントは全面的に使ってきたが、ゼロからのアプリ開発をコーディングエージェントで行ったのは初めて。

これをやってみて思ったことがある。

まず、日本のスタートアップでよくある「特定ユースケース用のエージェント」ああいうのはすぐに ChatGPT でできるじゃんということで廃れるのではないかと思う。

かといって様々なドメインに対応するバックエンドを保守運用することはバイブコーディングでは難しい。

なのでヘッドレスの SaaS が今後来るのではないかと思っているのだがそれも考えさせられる。

バックエンドは Firebase にすれば、インフラやバックエンドのことはあまり気にせずにアプリが作れてしまう。

一方で
